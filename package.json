{
    "name": "portfolio",
    "version": "1.0.0",
    "private": true,
    "homepage": "https://collin.diekvoss.com",
    "devDependencies": {
        "gh-pages": "^6.2.0"
    },
    "scripts": {
        "predeploy": "trunk build --release",
        "deploy": "gh-pages -d dist"
    }
}
